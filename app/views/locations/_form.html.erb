<%= form_for @location do |f| %>
	<%= f.label :city %>
	<%= f.text_field :city %>

		<%= f.label :country %>
	<%= f.text_field :country %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.submit %>
<% end %>
